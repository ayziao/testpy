language: python
python:
  - "3.3"
#  - pypy #pyhon3版出たら
install:
  - pip install coveralls --use-mirrors
script:
  - coverage run --source=myapp tests/all_test.py
  - coverage report -m
after_success:
  coveralls --verbose
