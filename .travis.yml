language: python
python:
  - "3.3"
# command to install dependencies
# install: "pip install -r requirements.txt --use-mirrors"
# command to run tests
script:
  - nosetests -w unitTest -w unitTest/common -v --with-coverage --with-id
